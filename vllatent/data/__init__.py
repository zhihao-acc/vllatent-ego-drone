"""TORCH tier — cached-latent dataset / loader.

Heavy imports (torch) are LAZY so a torch-free box imports this package without
crashing. Tested via `make test-torch` on tiny fixtures / dev box.
"""
